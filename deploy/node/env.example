# Copy to .env next to docker-compose.yml.

# Reusable Tailscale auth key (tailscale.com/admin/settings/keys).
# The container joins as its own machine; tag it however you like.
TS_AUTHKEY=tskey-auth-...
TS_HOSTNAME=brolink-node

# Optional. Generated on first start if empty.
BROLINK_USER=brolink
BROLINK_PASS=

SCREEN_SIZE=1920x1080x24
